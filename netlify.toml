[build]
  command = "npx vite build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: route every path to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
